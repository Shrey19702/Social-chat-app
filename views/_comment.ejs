<li id="comment-<%=comment._id%>">
    <p>
        <%= comment.user.name %> - <%= comment.content %>
        &emsp;
        <% if( (locals.user) && (locals.user.id == comment.user.id) ){%> 
            <a class="delete-comment-button" href="/posts/comments/deleteComment/<%=comment.id%>">X</a>
        <%}%>
    </p>
</li>